{
  "name": "project-diploma-performance-analysis",
  "type": "module",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "prebuild": "node scripts/copy_data.mjs",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test:unit": "vitest run",
    "verify:static": "node scripts/verify_static.mjs",
    "pagefind:index": "pagefind --site dist --output-path dist/pagefind && node scripts/copy_pagefind.mjs",
    "postbuild": "npm run pagefind:index && npm run verify:static"
  },
  "dependencies": {
    "@astrojs/svelte": "^7.0.0",
    "astro": "^5.0.0",
    "chart.js": "^4.4.1",
    "svelte": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "pagefind": "^1.3.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
